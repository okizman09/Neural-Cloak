# Optional heavy dependencies for stronger adversarial attacks and detectors
# Install into your venv only if you need white-box attacks or MTCNN embeddings
# Example: pip install -r requirements-optional.txt
torch>=1.13; platform_system != 'Windows' or platform_machine != 'AMD64'
facenet-pytorch>=2.5
face_recognition
